# v0.8.0 Implementation Tickets (C-8001..C-8003)

本ファイルは `docs/architecture/V0.8.0_DESIGN.md` の Contract Matrix を、実装可能な作業単位に分割したチケット一覧です。

## TKT-8001: Share JSON Receipt Hint (`C-8001`)

- Contract:
  - `zt send --share-json` payload に `receipt_hint.version/path/command` を追加
- 変更対象:
  - `gateway/zt/share_transport.go`
  - `gateway/zt/share_transport_test.go`
- 実装タスク:
  1. `receiverShareMessage` に receipt hint を追加
  2. `renderReceiverShareJSON` payload を additive 拡張
  3. JSON contract test で `receipt_hint` を必須検証
- Definition of Done:
  - 既存4フィールド互換を維持
  - JSON payload で `receipt_hint` が安定出力

## TKT-8002: Deterministic Receipt Path (`C-8002`)

- Contract:
  - packet basename から deterministic に receipt path を生成
- 変更対象:
  - `gateway/zt/share_transport.go`
  - `gateway/zt/commands_share_test.go`
- 実装タスク:
  1. `receiverSuggestedReceiptPath` を追加
  2. unsafe token を `_` へ正規化
  3. `receiverVerifyCommandWithReceipt` を追加
- Definition of Done:
  - 同一入力で path が常に同じ
  - non-packet 入力で空文字 fail-closed

## TKT-8003: Flow E2E Receipt Linkage (`C-8003`)

- Contract:
  - `share-json` の `receipt_hint` で verify+receipt 導線がつながる
- 変更対象:
  - `gateway/zt/flow_contract_test.go`
- 実装タスク:
  1. `receiverSharePayload` に `receipt_hint` を追加
  2. `receipt_hint.command` と `command` の artifact 整合を検証
  3. `receipt_hint.path` を使った `--receipt-out` receipt 生成を検証
- Definition of Done:
  - E2E contract が green
  - receipt schema (`v1`) は既存契約を維持

## 横断チケット: CI gate

- 追加ファイル:
  - `scripts/ci/check-v080-core-gate.sh`
- 更新:
  - `scripts/ci/check-pre-push-readiness.sh`
- Definition of Done:
  - pre-push で v0.8.0 契約回帰を独立検知できる
