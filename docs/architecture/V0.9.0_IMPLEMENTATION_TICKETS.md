# v0.9.0 Implementation Tickets (C-9001..C-9003)

本ファイルは `docs/architecture/V0.9.0_DESIGN.md` の Contract Matrix を、実装可能な作業単位に分割したチケット一覧です。

## TKT-9001: Share JSON Channel Templates (`C-9001`)

- Contract:
  - `zt send --share-json` payload に `channel_templates.version/slack_text/email_subject/email_body` を追加
- 変更対象:
  - `gateway/zt/share_transport.go`
  - `gateway/zt/share_transport_test.go`
- 実装タスク:
  1. `receiverShareMessage` に `channel_templates` を追加
  2. `renderReceiverShareJSON` payload を additive 拡張
  3. JSON contract test で `channel_templates` 必須検証
- Definition of Done:
  - 既存5フィールド互換を維持
  - JSON payload で `channel_templates` が安定出力

## TKT-9002: Localized Template Rendering (`C-9002`)

- Contract:
  - `ja|en` ローカライズと command linkage を固定
- 変更対象:
  - `gateway/zt/share_transport.go`
  - `gateway/zt/commands_share_test.go`
  - `gateway/zt/share_transport_test.go`
- 実装タスク:
  1. Slack/Email 向けテンプレート renderer を追加
  2. `format` に応じた文面切替を固定
  3. テンプレート本文に verify/receipt command を含める
- Definition of Done:
  - `format=en|ja` で期待言語が出力される
  - non-packet 入力で template builder が fail-closed

## TKT-9003: Flow E2E Non-regression (`C-9003`)

- Contract:
  - `send -> share-json -> verify -> receipt` 導線を維持しつつ `channel_templates` 欠落を検知
- 変更対象:
  - `gateway/zt/flow_contract_test.go`
- 実装タスク:
  1. flow payload struct に `channel_templates` を追加
  2. E2Eで template field の presence/linkage を検証
  3. receipt 契約 (`receipt_version=v1`) 非回帰を確認
- Definition of Done:
  - E2E contract が green
  - verify / receipt 導線は v0.8.0 契約を維持

## 横断チケット: CI gate

- 追加ファイル:
  - `scripts/ci/check-v090-core-gate.sh`
- 更新:
  - `scripts/ci/check-pre-push-readiness.sh`
- Definition of Done:
  - pre-push で v0.9.0 契約回帰を独立検知できる
